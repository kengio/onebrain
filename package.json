{
  "name": "onebrain",
  "version": "2.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bump": "bun run --cwd packages/cli bump",
    "test": "bun test packages/ || true",
    "build": "bun run --cwd packages/cli build"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9",
    "typescript": "^5.7"
  }
}
